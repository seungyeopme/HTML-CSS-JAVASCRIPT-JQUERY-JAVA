package personal;
/*
class MyMath{
	long add(long a, long b) {
		long result = a + b;
		return result;

	}
	long subtract(long a ,long b) { 
		long result = a - b;
		return result;
	}
	long multiply(long a, long b) { 
		long result = a * b;
		return result;
	}
	double divide(double a, double b) { 
		double result = a / b;
		return result;
	}
}


public class p7 {
	public static void main(String[] args) {

		MyMath mm = new MyMath();
		long result1 = mm.add(5L, 3L);
		long result2 = mm.subtract(5L, 3L);
		long result3 = mm.multiply(5L, 3L);
		double result4 = mm.divide(5L, 3L);	// double 대신 long값으로 호출하였다. 이 값은 double로 자동 형변환된다.
		
		System.out.println("add(5L, 3L) = " +result1);
		System.out.println("subtract(5L, 3L) = " +result2);
		System.out.println("multiply(5L, 3L) = " +result3);
		System.out.println("divide(5L, 3L) = " +result4);
	}
}


//사칙연산을 위한 4개의 메소드가 정의되어 있는 MyMath클래스를 이용한 예제이다. 이 예제를 통해서 클래스에 선언된 메서드를 어떻게
//호출하는 지 알 수 있을 것이다.
//여기서 눈여겨봐야 할 곳은 divide(double a, double b)를 호출하는 부분이다. divide메소드에 선언된 매개변수 타입은
//double형인데, 이와 다른 long형의 값인 5L과 3L을 사용해서 호출하는 것이 가능하다.
*/
